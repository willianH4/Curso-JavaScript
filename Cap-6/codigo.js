const input = document.querySelector(".input-normal");

document.write(input.className);

document.write(input.value); //muestra lo que contiene el input

// input.accept() permite seleccionar el tipo de archivos a cargar en un input tipo file

